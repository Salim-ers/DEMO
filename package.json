{
  "name": "studio-one",
  "private": true,
  "version": "0.1.0",
  "description": "Studio One — generate premium SaaS demo videos from a real browser capture pipeline.",
  "packageManager": "pnpm@9.12.0",
  "engines": { "node": ">=20.0.0" },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel --filter=@studio-one/web",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "worker": "pnpm --filter @studio-one/worker dev",
    "db:generate": "pnpm --filter @studio-one/db generate",
    "db:push": "pnpm --filter @studio-one/db push",
    "db:seed": "pnpm --filter @studio-one/db seed",
    "db:studio": "pnpm --filter @studio-one/db studio",
    "render:example": "pnpm --filter @studio-one/render render:example",
    "capture:install": "pnpm --filter @studio-one/capture exec playwright install chromium",
    "infra:up": "docker compose -f infra/docker-compose.yml up -d",
    "infra:down": "docker compose -f infra/docker-compose.yml down",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "devDependencies": {
    "turbo": "^2.1.3",
    "typescript": "^5.6.2",
    "prettier": "^3.3.3",
    "@types/node": "^22.5.5"
  },
  "pnpm": {
    "overrides": {
      "ioredis": "5.10.1"
    }
  }
}
